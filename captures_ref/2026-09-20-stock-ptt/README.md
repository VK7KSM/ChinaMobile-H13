# 2026-09-20 原厂发射的 AT 交互（只读抓取）

生产程序 `net.elfradio.h13interphone` 持有串口，操作者正常按一次 PTT
说话，抓 logcat 得到。这是一次**健康发射**的完整上位机交互，用作对照。

要点：开呼、开功放、取音频、编码、发射全部由 MCU 完成，上位机只下
`AT+DMOPTT=1`。发射音频来自模拟麦克风通路，期间 `audio_switch` 由系统
强制保持为 1。

详见 H13_new.md 的 2.8.68 与 2.8.69。
